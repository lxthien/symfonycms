<nav id="nav" role="navigation" aria-label="Menu chính">
    <div class="nav-container">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="navbar navbar-default">
                        <div class="navbar-header">
                            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
                                <span class="sr-only">Toggle navigation</span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </button>

                            <a class="navbar-brand hidden-sm hidden-md hidden-lg" href="{{ path('homepage') }}" title="{{ get_setting('siteName') }}">
                                <img src="{{ asset(convert_images.webpConvert2(asset('/assets/images/logo-mobile.png'), '')) }}" class="d-inline-block" alt="{{ get_setting('siteName') }}" width="55" height="45">
                            </a>
                        </div>

                        <div class="collapse navbar-collapse" id="navbar-collapse">
                            {{ knp_menu_render('main_menu', {
                                'allow_safe_labels': true,
                            })|raw }}
                            <form class="navbar-form navbar-right" action="{{ path('news_search') }}" method="get" onsubmit="return this.q.value.trim().length > 0" novalidate>
                                <div class="form-group search-group">
                                    <input name="q" type="text" class="form-control" placeholder="{{ 'search'|trans }}" required minlength="1" oninvalid="this.setCustomValidity(' ')" oninput="this.setCustomValidity('')">
                                    <button type="submit" class="btn-search"><i class="fas fa-search"></i></button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>
