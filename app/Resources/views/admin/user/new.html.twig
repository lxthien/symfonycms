{% extends 'admin/layout.html.twig' %}

{% block body_id 'admin-user-new' %}

{% block main %}
    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">
                <div class="col-lg-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title">Thêm user mới</strong>
                        </div>
                        <div class="card-body">
                            {{ include('admin/user/_form.html.twig', {
                                form: form,
                                button_label: 'Tạo user',
                            }, with_context = false) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
