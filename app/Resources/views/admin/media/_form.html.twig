{{ form_start(form, form_attr|default({})) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        <i class="fa fa-save" aria-hidden="true"></i> {{ button_label|default('Lưu media') }}
    </button>
    <a href="{{ path('admin_media_index') }}" class="btn btn-link">
        <i class="fa fa-list-alt" aria-hidden="true"></i> Quay lại thư viện
    </a>
{{ form_end(form) }}
