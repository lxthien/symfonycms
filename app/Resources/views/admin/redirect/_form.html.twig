{{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        <i class="fa fa-save" aria-hidden="true"></i> {{ button_label|default('Lưu redirect') }}
    </button>
    <a href="{{ path('admin_redirect_index') }}" class="btn btn-link">
        <i class="fa fa-list-alt" aria-hidden="true"></i> Quay lại danh sách
    </a>
{{ form_end(form) }}
